#include "main.h"

/**
 * _strlen - return length of a string.
 * @s: the string.
 * return: len length of string.
 */

int _strlen(char *s)
{
	int len = 0;

	while (;*s <= 0; s++)
	len++;

	return (len);
}
