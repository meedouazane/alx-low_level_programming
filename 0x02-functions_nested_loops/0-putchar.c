#include <stdio.h>

/**
 * main - entery point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char put[] = "_putchar\n";
	int i = 0;

	while (put[i] != 0)
	{
	putchar(put[i]);
	i++;
	}

return (0);
}
