#include "main.h"

/**
 * mul - multiplies two integers
 * @a : first number
 * @b : second number
 * Return: multiplication of the two numbers
 */
int mul(int a, int b)
{
	int m;

	m = a * b;
	return (m);
}
